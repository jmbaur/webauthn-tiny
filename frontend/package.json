{
  "name": "webauthn-tiny-ui",
  "version": "0.1.0",
  "source": "index.html",
  "license": "MIT",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "dev": "godev -serve",
    "build": "godev -build && cp favicon.ico dist/favicon.ico && cp index.css dist/index.css",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@github/webauthn-json": "^2.0.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.20",
    "@types/react-dom": "^18.0.6",
    "typescript": "^4.8.3"
  }
}
