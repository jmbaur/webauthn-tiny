{% comment %} TODO(jared): Use `render` when this is merged https://github.com/cobalt-org/liquid-rust/pull/472 {% endcomment %}
{% include "top" %}
<div>
	<button id="add-credential">&#x002B;</button>
	<label for="add-credential">Add credential</label>
</div>
<br>
<div>
	{% unless credentials == empty %}
		Existing credentials:
		<div>
			{% for cred in credentials %}
				<div>
					<button id="{{ cred.id }}" class="delete-credential" value="{{ cred.id }}">
						&#x2212;
					</button>
					<label for="{{ cred.id }}">{{ cred.name }}</label>
				</div>
			{% endfor %}
		</div>
	{% endunless %}
</div>
{% comment %} TODO(jared): Use `render` when this is merged https://github.com/cobalt-org/liquid-rust/pull/472 {% endcomment %}
{% include "bottom" %}
