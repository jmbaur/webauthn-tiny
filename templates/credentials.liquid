<!DOCTYPE html>
<html>
	<head>
		<link rel="stylesheet" href="/style.css">
		<title>WebAuthnTiny</title>
	</head>
	<body>
		<div>
			<form>
				<label for="new-cred-name">New credential:</label>
				<input type="text" placeholder="name" id="new-cred-name" name="new-cred-name">
				<input type="submit" value="Add">
			</form>
		</div>
		<div>
			{% unless credentials == empty %}
				Existing credentials:<br>
				{% for cred in credentials %}
					<button id="delete-button" value="{{ cred.id }}">
						&#x2212;
					</button>{{ cred.name }}
					{% comment %} TODO(jared): add label {% endcomment %}
				{% endfor %}
			{% endunless %}
		</div>
		<script src="/main.js"></script>
	</body>
</html>
